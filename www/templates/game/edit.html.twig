{% extends 'base.html.twig' %}

{% block title %}Edit Game Information{% endblock %}

{% block body %}
<h1>Edit game: {{ game.gameDate|date('Y-m-d H:i') }} by {{ game.teamName }}</h1>

<p>{{ include('game/_form.html.twig', {'button_label': 'Update'}) }}</p>
<p>{{ include('game/_delete_form.html.twig') }} </p>

<a href="{{ path('game_index') }}">
    <button type = "button" class = "btn btn-secondary">Back to list</button>
</a>
{% endblock %}