{% extends 'base.html.twig' %}

{% block title %}GameEvent{% endblock %}

{% block body %}
    <h1>GameEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_event.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_event.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ game_event.type }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ game_event.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_event_index') }}">back to list</a>

    <a href="{{ path('game_event_edit', {'id': game_event.id}) }}">edit</a>

    {{ include('game_event/_delete_form.html.twig') }}
{% endblock %}
