{% extends 'base.html.twig' %}

{% block title %}Edit Game {{ game.id }} Information{% endblock %}

{% block body %}
    <h2>Edit: {{ game.teamName }} game </h2>
    <h3>Date: {{ game.gameDate|date('Y-m-d H:i') }}</h3>

<p>{{ include('game/_form.html.twig', {'button_label': 'Update'}) }}</p>
<p>{{ include('game/_delete_form.html.twig') }} </p>

<a href="{{ path('game_index') }}">
    <button type = "button" class = "btn btn-primary">Back to list</button>
</a>
{% endblock %}